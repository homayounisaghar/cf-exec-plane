name: Personal Server Credential Rotation Stage1 (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Credential rotation Stage 1 is complete and archived. Use the current RUNBOOK rotation procedure."
