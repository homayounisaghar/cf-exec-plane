name: Onshape E9 Mate Timeout Reconcile
on:
  push:
    branches: [main]
    paths: ['server-bootstrap/control/onshape-e9-mate-reconcile/*.request']
permissions: {contents: read}
concurrency:
  group: onshape-e9-mate
  cancel-in-progress: false
jobs:
  reconcile:
    if: github.actor == 'homayounisaghar'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_PORT: ${{ secrets.VPS_PORT }}
      VPS_SSH_USER: ${{ secrets.VPS_SSH_USER }}
      VPS_HOST_KEY: ${{ secrets.VPS_HOST_KEY }}
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 2, persist-credentials: false}
      - name: Validate request
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t changes < <(git diff --name-status "${GITHUB_SHA}^" "$GITHUB_SHA")
          [[ "${#changes[@]}" -eq 1 ]]
          IFS=$'\t' read -r s p <<<"${changes[0]}"
          [[ "$s" == A && "$p" == server-bootstrap/control/onshape-e9-mate-reconcile/*.request ]]
          [[ "$(awk -F= '$1=="action"{print $2}' "$p" | tail -n1 | tr -d '[:space:]')" == "inspect-timeout" ]]
          bash -n server-bootstrap/218-onshape-e9-mate-reconcile.sh
      - name: Inspect durable execution state
        shell: bash
        run: bash server-bootstrap/runner/remote-bundle-exec.sh server-bootstrap/218-onshape-e9-mate-reconcile.sh
