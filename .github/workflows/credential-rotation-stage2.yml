name: Personal Server Credential Rotation Stage2 (Archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Credential rotation Stage 2 is complete and archived. Use the current RUNBOOK rotation procedure for the next rotation."
