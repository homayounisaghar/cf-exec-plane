name: Onshape A2 Android Attestation Gate

on:
  push:
    branches: [main]
    paths:
      - 'server-bootstrap/control/onshape-a2-attestation-gate/*.request'

permissions:
  contents: read

concurrency:
  group: onshape-a2-attestation-gate
  cancel-in-progress: false

jobs:
  prove:
    if: github.actor == 'homayounisaghar'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      CF_REPO_READ_TOKEN: ${{ secrets.CF_REPO_READ_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          persist-credentials: false
      - name: Validate immutable A2 attestation request
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t changes < <(git diff --name-status "${GITHUB_SHA}^" "$GITHUB_SHA")
          [[ "${#changes[@]}" -eq 1 ]] || exit 10
          IFS=$'\t' read -r status request_path <<<"${changes[0]}"
          [[ "$status" == A ]] || exit 11
          [[ "$request_path" == server-bootstrap/control/onshape-a2-attestation-gate/*.request ]] || exit 12
          request_id="$(awk -F= '$1=="request_id"{print $2}' "$request_path" | tail -n1 | tr -d '[:space:]')"
          action="$(awk -F= '$1=="action"{print $2}' "$request_path" | tail -n1 | tr -d '[:space:]')"
          [[ "$request_id" == "$(basename "$request_path" .request)" ]] || exit 13
          [[ "$action" == "prove-android-attestation-gate" ]] || exit 14
      - name: Fetch canonical control and current Android attestation
        shell: bash
        run: |
          set -euo pipefail
          tmp="$RUNNER_TEMP/a2-attestation"
          mkdir -p "$tmp"
          cat >"$tmp/askpass" <<'ASK'
          #!/usr/bin/env bash
          case "${1:-}" in
            *Username*) printf '%s\n' x-access-token ;;
            *Password*) printf '%s\n' "$CF_REPO_READ_TOKEN" ;;
            *) exit 1 ;;
          esac
          ASK
          chmod 0700 "$tmp/askpass"
          git init --bare "$tmp/repo.git" >/dev/null
          GIT_ASKPASS="$tmp/askpass" GIT_TERMINAL_PROMPT=0 git --git-dir="$tmp/repo.git" fetch --quiet --depth=1             https://github.com/homayounisaghar/capability-fabric.git refs/heads/main:refs/heads/main
          git --git-dir="$tmp/repo.git" show refs/heads/main:runtime/onshape/ONSHAPE_RUNTIME_CONTROL.json >"$tmp/control.json"
          code="$(curl -sS -o "$tmp/issue.json" -w '%{http_code}'             -H "Authorization: Bearer $CF_REPO_READ_TOKEN"             -H 'Accept: application/vnd.github+json'             https://api.github.com/repos/homayounisaghar/capability-fabric/issues/33)"
          [[ "$code" == 200 ]] || { echo "CF_A2_ATTESTATION_ISSUE_READ_HTTP=$code" >&2; exit 20; }
          python3 - "$tmp/issue.json" "$tmp/body.txt" <<'PY'
          import json,sys
          v=json.load(open(sys.argv[1]))
          body=v.get("body")
          assert isinstance(body,str) and body
          open(sys.argv[2],"w").write(body)
          print("CF_A2_ATTESTATION_ISSUE_READ=pass")
          PY
          python3 server-bootstrap/113-onshape-android-close-attestation-verify.py current "$tmp/control.json" "$tmp/body.txt"
          set +e
          out="$(python3 server-bootstrap/113-onshape-android-close-attestation-verify.py closed "$tmp/control.json" "$tmp/body.txt" 2>&1)"
          rc=$?
          set -e
          printf '%s\n' "$out"
          [[ "$rc" -eq 30 ]]
          printf '%s\n' "$out" | grep -Fxq 'CF_ANDROID_CLOSE_ATTESTATION=not-closed'
          echo CF_A2_CLOSE_GATE_REJECTS_CURRENT_OPEN_ROUTE=pass
      - name: Prove exact closed fixture is accepted
        shell: bash
        run: |
          set -euo pipefail
          tmp="$RUNNER_TEMP/a2-attestation"
          python3 - "$tmp/control.json" "$tmp/body.txt" "$tmp/closed-control.json" "$tmp/closed-body.txt" <<'PY'
          import hashlib,json,re,sys
          cp,bp,oc,ob=sys.argv[1:]
          root=json.load(open(cp))
          root["controlRevision"]+=1
          root["updatedAt"]="2026-09-21T00:00:00.000Z"
          root["routing"]["state"]="CLOSED"
          root["routing"]["materialCommandsAllowed"]=False
          a=root["authority"]
          a["productionEpoch"]+=1
          a["mode"]="QUIESCED_RECONCILING"
          a["materialAuthority"]=None
          a["planes"]["android-v1"]["ingress"]="CLOSED"
          a["planes"]["android-v1"]["materialEffectsAllowed"]=False
          a["planes"]["vps-fabric"]["ingress"]="CLOSED"
          a["planes"]["vps-fabric"]["materialEffectsAllowed"]=False
          raw=(json.dumps(root,indent=2)+"\n").encode()
          open(oc,"wb").write(raw)
          sha=hashlib.sha1(f"blob {len(raw)}\0".encode()+raw).hexdigest()
          body=open(bp).read()
          p=body.find("CF_ONSHAPE_RUNTIME_STATUS_V1")
          start=body.find("{",p)
          status,end=json.JSONDecoder().raw_decode(body[start:])
          status["state"]="BLOCKED"
          status["routingState"]="CLOSED"
          status["materialCommandsAllowed"]=False
          status["observedRuntimeControlSha"]=sha
          status["controlRevision"]=root["controlRevision"]
          status["busGeneration"]=root["routing"]["busGeneration"]
          status["deviceAdmittedBusGeneration"]=root["routing"]["busGeneration"]
          status["deviceAdmittedMailboxIssue"]=root["routing"]["activeMailboxIssue"]
          status["leaseState"]="FREE"
          status["queueDepth"]=0
          status["currentCommandSourceId"]=None
          newbody="CF_ONSHAPE_RUNTIME_STATUS_V1\n"+json.dumps(status)
          open(ob,"w").write(newbody)
          PY
          python3 server-bootstrap/113-onshape-android-close-attestation-verify.py closed "$tmp/closed-control.json" "$tmp/closed-body.txt"
          echo CF_A2_CLOSE_GATE_FIXTURE=pass
